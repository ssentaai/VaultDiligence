# External Sources Registry — Pending

Candidate sources awaiting research or operator decision. Schema:
`spec/external-sources-schema.md`. Workflow: `.claude/skills/source-add/SKILL.md`.

The `source-discovery` agent PROPOSES entries here (Shape 1 — agents Pend,
operator Registers; see DECISIONS 2026-06-19). Items here are then registered by
the operator via `source-add` Step 5, rejected to `_rejected.md`, or removed when
stale (>90 days without progress).

Proposed entries from the agent are schema-valid (frontmatter cloning
`accountable-data-feeds.md`, `status: candidate`, all 7 body sections). A lighter
research-stub form is also valid here:

```

(none yet)
